using System;
using Core.Classes;
using Microsoft.AspNetCore.Http;
using Xunit;

namespace Tests
{
    public class ImageUploadServiceTests
    {
        /* [Theory]
        [InlineData(new byte[]{}, true)]
        public void ImageUploadServiceTests_UploadImage_Should_ReturnsImageName(IFormFile imagem, string imageName)
        {
            //Arrange
            var service = new ImageUploadService();

            //Act
            var result = service.UploadImage(imagem);

            //Assert
            Assert.Equal(result.Result, imageName);
        }

        [Theory]
        [InlineData(new byte[]{}, true)]
        public void ImageUploadServiceTests_SaveImageInDisk_Should_ReturnsImageName(IFormFile imagem, string imageName)
        {
            //Arrange
            var service = new ImageUploadService();

            //Act
            var result = service.UploadImage(imagem);

            //Assert
            Assert.Equal(result.Result, imageName);
        } */
    }
}
